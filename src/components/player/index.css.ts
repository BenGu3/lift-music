import { css } from '../../../styled-system/css'

export const emptyPlayerContainer = css({
    marginTop: '48px',
    width: '60%',
    color: '#125ec8',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
})

export const emptyPlayerTitle = css({
    marginBottom: '48px',
    fontWeight: '600',
    fontSize: '48px',
})

export const emptyPlayerMotto = css({
    fontSize: '20px',
})

export const playerContainer = css({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginTop: '24px',
    width: '60%',
})

export const playerContent = css({
    display: 'flex',
    marginTop: '24px',
})

export const player = css({
    border: '0',
    height: '240px',
})
